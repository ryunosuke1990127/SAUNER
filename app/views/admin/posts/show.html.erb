<html>
  <head>
     <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>
  <body>
    <div class="container">
       <div class="row">
        <div class="col-md-3">

        </div>

        <div class="col-md-8 offset-md-1">
          <div>投稿サウナ<%=@post.name%></div>
          <div>投稿日<%=@post.updated_at%></div>
          <div>地域<%=@post.region.region_name%></div>
          <div>所在地<%=@post.location%></div>
          <div>金額　¥<%=@post.price%></div>
          <div>投稿理由<%=@post.post_reason%></div>
      　<%=link_to "却下"  ,admin_post_path(@post.id), method: :DELETE, class: 'btn btn-danger' %>
        <%=link_to "投稿"  ,admin_post_path(@post.id) , method: :PATCH, class: 'btn btn-success' %>
        </div>
      </div>
    </div>
  </body>
</html>
